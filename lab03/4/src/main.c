// Дано 4-розрядне число у системі числення р. Визначити його еквівалент у десятковій системі числення. 
int main()
{ 
       int p = 8; // восьмеричная cистема счисления
       int a = 1234; // 4-разрядное число, представленное в системе счисления  p
       int p10 = (((a-(a%1000))/1000)*p*p*p)+((((a%1000)-a%100)/100)*p*p)+((((a%100)-(a%10))/10)*p)+(a%10); // еквивалент числа а в десятичной системе счисления
       return 0;
}
